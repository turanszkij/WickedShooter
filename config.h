#pragma once

namespace config
{
	static constexpr const char* const main_script_file = "first_person.lua"; // name of the script file near the exe to load at start
	static constexpr wi::Color backlog_color = wi::Color(130, 250, 180, 255); // color of backlog font (startup text)
}
